<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.swpu.info.college_website.dao.BannerImgInfoDao">

    <!-- banner_img_Info 所有查询列 -->
    <sql id="QUERY_COLUMN_LIST">
		<![CDATA[
        id, banner_name_type As BannerNameType, url, describe_info AS describeInfo, create_date AS createDate, modify_date AS modifyDate
        ]]>
	</sql>
    <!-- banner_img_info 查询列来源表-->
    <sql id="QUERY_FROM_TABLE">
        <![CDATA[
        FROM banner_img_info
        ]]>
    </sql>
    <!-- 全部条件(更多功能可以通过queryData扩展实现)  -->
    <sql id="QUERY_WHERE_CLAUSE">
        <where>
            <if test="id != null and id != ''"><![CDATA[AND id = #{id}]]></if>
            <if test="bannerNameType != null and bannerNameType != ''">
                <![CDATA[AND banner_name_type = #{bannerNameType}]]></if>
            <if test="url != null and url != ''"><![CDATA[AND url = #{url}]]></if>
            <if test="describeInfo != null and describeInfo != ''"><![CDATA[AND describe_Info = #{describeInfo}]]></if>
            <if test="createDate != null and createDate != ''"><![CDATA[AND create_date = #{createDate}]]></if>
            <if test="modifyDate != null and modifyDate != ''"><![CDATA[AND modify_date = #{modifyDate}]]></if>
        </where>
    </sql>
    <!-- 智能排序与分页 -->
    <sql id="QUERY_ORDER_LIMIT_CONDTION">
        <if test="orderField != null and orderField != '' and orderFieldType != null and orderFieldType != ''">
            <![CDATA[ORDER BY ${orderField} ${orderFieldType}]]></if>
        <if test="startIndex != null and startIndex &gt;= 0 and pageSize != null and pageSize &gt; 0">
            <![CDATA[LIMIT #{startIndex},#{pageSize}]]></if>
    </sql>
    <!-- 更新列字段,只要不为NULL则更新,除开主键列 -->
    <sql id="UPDATE_COLUMN_SET">
        <set>
            <if test="id != null"><![CDATA[id = #{id},]]></if>
            <if test="bannerNameType != null and bannerNameType != ''">
                <![CDATA[AND banner_name_type = #{bannerNameType},]]></if>
            <if test="describeInfo != null and describeInfo != ''"><![CDATA[AND describe_Info = #{describeInfo},]]></if>
            <if test="createDate != null"><![CDATA[create_date = #{createDate},]]></if>
            <if test="modifyDate != null"><![CDATA[modify_date = #{modifyDate},]]></if>
        </set>
    </sql>
    <!-- 插入banner_img_info记录 -->
    <insert id="insertEntry" parameterType="userInfo">
		<![CDATA[
        INSERT INTO banner_img_info (id, banner_name_type, url, describe_Info)
        VALUES (#{id}, #{bannerNameType}, #{url}, #{describeInfo})
        ]]>
	</insert>
    <!-- 返回插入的编号,在事务开启状态下有效 -->
    <select id="lastSequence" resultType="long"><![CDATA[
        SELECT LAST_INSERT_ID() AS id
        ]]></select>
    <!-- 删除记录,主键IN(array) -->
    <delete id="deleteByArrayKey" parameterType="java.lang.reflect.Array">
        <![CDATA[DELETE FROM ops_function WHERE id IN]]>
        <foreach collection="array" item="id" open="(" separator="," close=")">
            <![CDATA[#{id}]]>
        </foreach>
    </delete>
    <!-- 删除,通过条件 -->
    <update id="deleteByCondtion" parameterType="bannerImgInfo">
        <![CDATA[DELETE FROM banner_img_info]]>
        <include refid="QUERY_WHERE_CLAUSE"/>
    </update>

    <!-- 修改记录通过主键 -->
    <update id="updateByKey" parameterType="bannerImgInfo">
        <![CDATA[UPDATE banner_img_info]]>
        <include refid="UPDATE_COLUMN_SET"/>
        <![CDATA[WHERE id = #{id}]]>
    </update>

    <!-- 查询,通过主键IN(array) -->
    <select id="selectEntryArray" parameterType="java.lang.reflect.Array" resultType="bannerImgInfo">
        <![CDATA[SELECT]]>
        <include refid="QUERY_COLUMN_LIST"/>
        <include refid="QUERY_FROM_TABLE"/>
        <![CDATA[WHERE id IN]]>
        <foreach collection="array" item="id" open="(" separator="," close=")">
            <![CDATA[#{id}]]>
        </foreach>
    </select>

    <!-- 查询,通过条件 -->
    <select id="selectEntryList" parameterType="bannerImgInfo" resultType="bannerImgInfo">
        <![CDATA[SELECT]]>
        <include refid="QUERY_COLUMN_LIST"/>
        <include refid="QUERY_FROM_TABLE"/>
        <include refid="QUERY_WHERE_CLAUSE"/>
        <include refid="QUERY_ORDER_LIMIT_CONDTION"/>
    </select>

    <!-- 总数查询,通过条件 -->
    <select id="selectEntryListCount" parameterType="bannerImgInfo" resultType="int">
        <![CDATA[SELECT COUNT(id) AS dataCount]]>
        <include refid="QUERY_FROM_TABLE"/>
        <include refid="QUERY_WHERE_CLAUSE"/>
    </select>

    <!-- 其它SQL语句 -->
</mapper>